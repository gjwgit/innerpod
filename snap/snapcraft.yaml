name: innerpod
version: 1.8.6
summary: A session timer with session logged to your Solid Pod
description: |
  A session timer typically used for silent meditation.

base: core24
grade: stable
confinement: strict

apps:
  innerpod:
    command: innerpod
    extensions: [gnome] # gnome includes the libraries required by flutter
    plugs:
      - audio-playback
      - desktop
      - desktop-legacy
      - pulseaudio
      - wayland
      - x11
      - opengl
      - network
      - home
      - screen-inhibit-control
      - network-bind
      - password-manager-service

# 20251013 gjw The `flutter_secure_storage` package (and maybe
# `vault_storage`) is used by `solidpod/solidui` to save secrets
# locally, if the app provides this option to the user to do so. Thus
# `libsecret-1-dev`, `libjsoncpp-dev`, `llvm`, and `lld` at least are
# required to build the installer and `libsecret-1-0` and
# `libjsoncpp1` to run the application. There may be more that are
# included below and need to test their removal to test. See
#
# https://github.com/juliansteenbakker/flutter_secure_storage/blob/develop/README.md
# https://pub.dev/packages/vault_storage/versions/2.2.0#platform-setup
#
#    build-packages:
#      - libsecret-1-dev
#      - libjsoncpp-dev
#      - llvm
#      - lld
#
#    stage-packages:
#      - libsecret-1-0
#      - libjsoncpp-dev
#
# 20251013 gjw If using the `audioplayers` package for flutter then
# the gstreamer packages are required for bulding the installer
# running the app.
#
#    build-packages:
#      - libgstreamer1.0-dev
#      - libgstreamer-plugins-base1.0-dev
#      - libgstreamer-plugins-good1.0-dev
#    stage-packages:
#      - libgstreamer1.0-0
#      - libgstreamer-plugins-base1.0-0
#      - libgstreamer-plugins-good1.0-0
#      - gstreamer1.0-plugins-good
#      - gstreamer1.0-plugins-base

parts:
  innerpod:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart
    build-packages:
      - clang
      - cmake
      - llvm
      - git
      - pkg-config
      - ninja-build
      - libgtk-3-dev
      - libblkid-dev
      - liblzma-dev
      - libgstreamer1.0-dev
      - libgstreamer-plugins-base1.0-dev
      - libgstreamer-plugins-good1.0-dev
      - libsecret-1-dev
      - libjsoncpp-dev
      - libssl-dev
      - lld
      - binutils
    stage-packages:
      - libgstreamer1.0-0
      - libgstreamer-plugins-base1.0-0
      - libgstreamer-plugins-good1.0-0
      - gstreamer1.0-plugins-good
      - gstreamer1.0-plugins-base
      - libsecret-1-0
      - libjsoncpp-dev
      - libssl3
      - libgtk-3-0
